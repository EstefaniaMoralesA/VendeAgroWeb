@model VendeAgroWeb.Models.Administrador.AnunciosViewModel

<div class="table-responsive">
    <table class="table table-striped table-responsive">
        <thead>
            <tr>
                <th>Activos</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @{
                foreach (var item in Model.AnunciosActivos)
                {
                    <tr>
                    </tr>
                }          
            }
        </tbody>
    </table>
</div>